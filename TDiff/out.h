// Generated by tkgen - Version Dec 17 2009

#ifndef __OUT_H__
#define __OUT_H__

namespace MGrammar
{

    enum Tokens
    {
        NEWLINE,
        SPACES,
        TAB,
        DEFINE,
        NOWIKI_BLOCK_CLOSE,
        CHAR,
        STRING,
        KY_ASM,
        KY_AUTO,
        KY_BOOL,
        KY_BREAK,
        KY_CASE,
        KY_CATCH,
        KY_CHAR,
        KY_CLASS,
        KY_CONST,
        KY_CONST_CAST,
        KY_CONTINUE,
        KY_DEFAULT,
        KY_DELETE,
        KY_DO,
        KY_DOUBLE,
        KY_DYNAMIC_CAST,
        KY_ELSE,
        KY_ENUM,
        KY_EXPLICIT,
        KY_EXPORT,
        KY_EXTERN,
        KY_FALSE,
        KY_FLOAT,
        KY_FOR,
        KY_FRIEND,
        KY_GOTO,
        KY_IF,
        KY_INLINE,
        KY_INT,
        KY_LONG,
        KY_MUTABLE,
        KY_NAMESPACE,
        KY_NEW,
        KY_OPERATOR,
        KY_PRIVATE,
        KY_PROTECTED,
        KY_PUBLIC,
        KY_REGISTER,
        KY_REINTERPRET_CAST,
        KY_RETURN,
        KY_SHORT,
        KY_SIGNED,
        KY_SIZEOF,
        KY_STATIC,
        KY_STATIC_CAST,
        KY_STRUCT,
        KY_SWITCH,
        KY_TEMPLATE,
        KY_THIS,
        KY_THROW,
        KY_TRUE,
        KY_TRY,
        KY_TYPEDEF,
        KY_TYPEID,
        KY_TYPENAME,
        KY_UNION,
        KY_UNSIGNED,
        KY_USING,
        KY_VIRTUAL,
        KY_VOID,
        KY_VOLATILE,
        KY_WCHAR_T,
        KY_WHILE,
        KY_NULL,
        KY_CONSTEXPR,
        KY_DECLTYPE,
        KY_STATIC_ASSERT,
        KY_ALIGNOF,
        PT_NOT,
        PT_PERCENT,
        PT_NOTXOT,
        PT_AND,
        PT_MULTI,
        PT_OPENP,
        PT_CLOSEP,
        PT_MINUS,
        PT_PLUS,
        PT_EQUAL,
        PT_OPENB,
        PT_CLOSEB,
        PT_OR,
        PT_NOTBIT,
        PT_OPENBL,
        PT_CLOSEBL,
        PT_SLASH,
        PT_DOTCOMA,
        PT_SINGLEQ,
        PT_DOTDOT,
        PT_DOUBLEQ,
        PT_LESSTHAN,
        PT_MORETHAN,
        PT_QUESTION,
        PT_COMA,
        PT_DOT,
        PT_INVSLASH,
        PT_NUMBER,
        NUMBER,
        SYMBOL,
        INSIGNIFICANT_CHAR,
        T_EOF,
    };

    inline const wchar_t* TokensToString(Tokens e)
    {
        switch (e)
        {
        case NEWLINE: return L"NEWLINE";
        case SPACES: return L"SPACES";
        case TAB: return L"TAB";
        case DEFINE: return L"DEFINE";
        case NOWIKI_BLOCK_CLOSE: return L"NOWIKI_BLOCK_CLOSE";
        case CHAR: return L"CHAR";
        case STRING: return L"STRING";
        case KY_ASM: return L"KY_ASM";
        case KY_AUTO: return L"KY_AUTO";
        case KY_BOOL: return L"KY_BOOL";
        case KY_BREAK: return L"KY_BREAK";
        case KY_CASE: return L"KY_CASE";
        case KY_CATCH: return L"KY_CATCH";
        case KY_CHAR: return L"KY_CHAR";
        case KY_CLASS: return L"KY_CLASS";
        case KY_CONST: return L"KY_CONST";
        case KY_CONST_CAST: return L"KY_CONST_CAST";
        case KY_CONTINUE: return L"KY_CONTINUE";
        case KY_DEFAULT: return L"KY_DEFAULT";
        case KY_DELETE: return L"KY_DELETE";
        case KY_DO: return L"KY_DO";
        case KY_DOUBLE: return L"KY_DOUBLE";
        case KY_DYNAMIC_CAST: return L"KY_DYNAMIC_CAST";
        case KY_ELSE: return L"KY_ELSE";
        case KY_ENUM: return L"KY_ENUM";
        case KY_EXPLICIT: return L"KY_EXPLICIT";
        case KY_EXPORT: return L"KY_EXPORT";
        case KY_EXTERN: return L"KY_EXTERN";
        case KY_FALSE: return L"KY_FALSE";
        case KY_FLOAT: return L"KY_FLOAT";
        case KY_FOR: return L"KY_FOR";
        case KY_FRIEND: return L"KY_FRIEND";
        case KY_GOTO: return L"KY_GOTO";
        case KY_IF: return L"KY_IF";
        case KY_INLINE: return L"KY_INLINE";
        case KY_INT: return L"KY_INT";
        case KY_LONG: return L"KY_LONG";
        case KY_MUTABLE: return L"KY_MUTABLE";
        case KY_NAMESPACE: return L"KY_NAMESPACE";
        case KY_NEW: return L"KY_NEW";
        case KY_OPERATOR: return L"KY_OPERATOR";
        case KY_PRIVATE: return L"KY_PRIVATE";
        case KY_PROTECTED: return L"KY_PROTECTED";
        case KY_PUBLIC: return L"KY_PUBLIC";
        case KY_REGISTER: return L"KY_REGISTER";
        case KY_REINTERPRET_CAST: return L"KY_REINTERPRET_CAST";
        case KY_RETURN: return L"KY_RETURN";
        case KY_SHORT: return L"KY_SHORT";
        case KY_SIGNED: return L"KY_SIGNED";
        case KY_SIZEOF: return L"KY_SIZEOF";
        case KY_STATIC: return L"KY_STATIC";
        case KY_STATIC_CAST: return L"KY_STATIC_CAST";
        case KY_STRUCT: return L"KY_STRUCT";
        case KY_SWITCH: return L"KY_SWITCH";
        case KY_TEMPLATE: return L"KY_TEMPLATE";
        case KY_THIS: return L"KY_THIS";
        case KY_THROW: return L"KY_THROW";
        case KY_TRUE: return L"KY_TRUE";
        case KY_TRY: return L"KY_TRY";
        case KY_TYPEDEF: return L"KY_TYPEDEF";
        case KY_TYPEID: return L"KY_TYPEID";
        case KY_TYPENAME: return L"KY_TYPENAME";
        case KY_UNION: return L"KY_UNION";
        case KY_UNSIGNED: return L"KY_UNSIGNED";
        case KY_USING: return L"KY_USING";
        case KY_VIRTUAL: return L"KY_VIRTUAL";
        case KY_VOID: return L"KY_VOID";
        case KY_VOLATILE: return L"KY_VOLATILE";
        case KY_WCHAR_T: return L"KY_WCHAR_T";
        case KY_WHILE: return L"KY_WHILE";
        case KY_NULL: return L"KY_NULL";
        case KY_CONSTEXPR: return L"KY_CONSTEXPR";
        case KY_DECLTYPE: return L"KY_DECLTYPE";
        case KY_STATIC_ASSERT: return L"KY_STATIC_ASSERT";
        case KY_ALIGNOF: return L"KY_ALIGNOF";
        case PT_NOT: return L"PT_NOT";
        case PT_PERCENT: return L"PT_PERCENT";
        case PT_NOTXOT: return L"PT_NOTXOT";
        case PT_AND: return L"PT_AND";
        case PT_MULTI: return L"PT_MULTI";
        case PT_OPENP: return L"PT_OPENP";
        case PT_CLOSEP: return L"PT_CLOSEP";
        case PT_MINUS: return L"PT_MINUS";
        case PT_PLUS: return L"PT_PLUS";
        case PT_EQUAL: return L"PT_EQUAL";
        case PT_OPENB: return L"PT_OPENB";
        case PT_CLOSEB: return L"PT_CLOSEB";
        case PT_OR: return L"PT_OR";
        case PT_NOTBIT: return L"PT_NOTBIT";
        case PT_OPENBL: return L"PT_OPENBL";
        case PT_CLOSEBL: return L"PT_CLOSEBL";
        case PT_SLASH: return L"PT_SLASH";
        case PT_DOTCOMA: return L"PT_DOTCOMA";
        case PT_SINGLEQ: return L"PT_SINGLEQ";
        case PT_DOTDOT: return L"PT_DOTDOT";
        case PT_DOUBLEQ: return L"PT_DOUBLEQ";
        case PT_LESSTHAN: return L"PT_LESSTHAN";
        case PT_MORETHAN: return L"PT_MORETHAN";
        case PT_QUESTION: return L"PT_QUESTION";
        case PT_COMA: return L"PT_COMA";
        case PT_DOT: return L"PT_DOT";
        case PT_INVSLASH: return L"PT_INVSLASH";
        case PT_NUMBER: return L"PT_NUMBER";
        case NUMBER: return L"NUMBER";
        case SYMBOL: return L"SYMBOL";
        case INSIGNIFICANT_CHAR: return L"INSIGNIFICANT_CHAR";
        case T_EOF: return L"T_EOF";
        default:break;
        }
        return L"";
    }

    struct MGrammar
    {
        typedef Tokens TokenType;

        static int GetNext(int state, wchar_t ch)
        {
            switch (state)
            {
            case 0:
                if (ch == L'\0')
                    return 1;
                else if (ch == L'\t')
                    return 2;
                else if (ch == L'\n')
                    return 3;
                else if (ch == L'\r')
                    return 4;
                else if (ch == L' ')
                    return 5;
                else if (ch == L'!')
                    return 6;
                else if (ch == L'"')
                    return 7;
                else if (ch == L'#')
                    return 8;
                else if (ch == L'$')
                    return 9;
                else if (ch == L'%')
                    return 10;
                else if (ch == L'&')
                    return 11;
                else if (ch == L'\'')
                    return 12;
                else if (ch == L'(')
                    return 13;
                else if (ch == L')')
                    return 14;
                else if (ch == L'*')
                    return 15;
                else if (ch == L'+')
                    return 16;
                else if (ch == L',')
                    return 17;
                else if (ch == L'-')
                    return 18;
                else if (ch == L'.')
                    return 19;
                else if (ch == L'/')
                    return 20;
                else if (ch >= L'0' && ch <= L'9')
                    return 21;
                else if (ch == L':')
                    return 22;
                else if (ch == L';')
                    return 23;
                else if (ch == L'<')
                    return 24;
                else if (ch == L'=')
                    return 25;
                else if (ch == L'>')
                    return 26;
                else if (ch == L'?')
                    return 27;
                else if (ch == L'@')
                    return 28;
                else if (ch >= L'A' && ch <= L'Z')
                    return 9;
                else if (ch == L'[')
                    return 29;
                else if (ch == L'\\')
                    return 30;
                else if (ch == L']')
                    return 31;
                else if (ch == L'^')
                    return 32;
                else if (ch == L'_')
                    return 9;
                else if (ch == L'`')
                    return 28;
                else if (ch == L'a')
                    return 33;
                else if (ch == L'b')
                    return 34;
                else if (ch == L'c')
                    return 35;
                else if (ch == L'd')
                    return 36;
                else if (ch == L'e')
                    return 37;
                else if (ch == L'f')
                    return 38;
                else if (ch == L'g')
                    return 39;
                else if (ch == L'h')
                    return 9;
                else if (ch == L'i')
                    return 40;
                else if (ch >= L'j' && ch <= L'k')
                    return 9;
                else if (ch == L'l')
                    return 41;
                else if (ch == L'm')
                    return 42;
                else if (ch == L'n')
                    return 43;
                else if (ch == L'o')
                    return 44;
                else if (ch == L'p')
                    return 45;
                else if (ch == L'q')
                    return 9;
                else if (ch == L'r')
                    return 46;
                else if (ch == L's')
                    return 47;
                else if (ch == L't')
                    return 48;
                else if (ch == L'u')
                    return 49;
                else if (ch == L'v')
                    return 50;
                else if (ch == L'w')
                    return 51;
                else if (ch >= L'x' && ch <= L'z')
                    return 9;
                else if (ch == L'{')
                    return 52;
                else if (ch == L'|')
                    return 53;
                else if (ch == L'}')
                    return 54;
                else if (ch == L'~')
                    return 55;
                else //if (ch == L'\u007f')
                    return 28;
                break; // 
            case 1:
                //T_EOF
                break; // 
            case 2:
                //TAB
                break; // 
            case 3:
                //NEWLINE
                break; // 
            case 4:
                if (ch == L'\n')
                    return 3;
                break; // 
            case 5:
                if (ch == L' ')
                    return 57;
                //SPACES
                //SPACES
                break; // 
            case 6:
                //PT_NOT
                //PT_NOT
                break; // 
            case 7:
                if (ch >= L' ' && ch <= L'!')
                    return 58;
                else if (ch == L'"')
                    return 59;
                else if (ch >= L'#' && ch <= L'[')
                    return 58;
                else if (ch == L'\\')
                    return 60;
                else if (ch >= L']' /*&& ch <= L'\u007f'*/)
                    return 58;
                //PT_DOUBLEQ
                //PT_DOUBLEQ
                break; // 
            case 8:
                if (ch == L' ')
                    return 61;
                else if (ch >= L'A' && ch <= L'Z')
                    return 62;
                else if (ch >= L'a' && ch <= L'z')
                    return 62;
                //PT_NUMBER
                //PT_NUMBER
                break; // 
            case 9:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 10:
                //PT_PERCENT
                //PT_PERCENT
                break; // 
            case 11:
                //PT_AND
                //PT_AND
                break; // 
            case 12:
                if (ch >= L' ' && ch <= L'&')
                    return 65;
                else if (ch == L'\'')
                    return 66;
                else if (ch >= L'(' && ch <= L'[')
                    return 65;
                else if (ch == L'\\')
                    return 67;
                else if (ch >= L']' /*&& ch <= L'\u007f'*/)
                    return 65;
                //PT_SINGLEQ
                //PT_SINGLEQ
                break; // 
            case 13:
                //PT_OPENP
                //PT_OPENP
                break; // 
            case 14:
                //PT_CLOSEP
                //PT_CLOSEP
                break; // 
            case 15:
                //PT_MULTI
                //PT_MULTI
                break; // 
            case 16:
                if (ch >= L'0' && ch <= L'9')
                    return 68;
                //PT_PLUS
                //PT_PLUS
                break; // 
            case 17:
                //PT_COMA
                //PT_COMA
                break; // 
            case 18:
                if (ch >= L'0' && ch <= L'9')
                    return 68;
                //PT_MINUS
                //PT_MINUS
                break; // 
            case 19:
                //PT_DOT
                //PT_DOT
                break; // 
            case 20:
                //PT_INVSLASH
                //PT_INVSLASH
                break; // 
            case 21:
                if (ch == L'.')
                    return 69;
                else if (ch >= L'0' && ch <= L'9')
                    return 68;
                //NUMBER
                //NUMBER
                break; // 
            case 22:
                //PT_DOTDOT
                //PT_DOTDOT
                break; // 
            case 23:
                //PT_DOTCOMA
                //PT_DOTCOMA
                break; // 
            case 24:
                //PT_LESSTHAN
                //PT_LESSTHAN
                break; // 
            case 25:
                //PT_EQUAL
                //PT_EQUAL
                break; // 
            case 26:
                //PT_MORETHAN
                //PT_MORETHAN
                break; // 
            case 27:
                //PT_QUESTION
                //PT_QUESTION
                break; // 
            case 28:
                //INSIGNIFICANT_CHAR
                break; // 
            case 29:
                //PT_OPENBL
                //PT_OPENBL
                break; // 
            case 30:
                //PT_SLASH
                //PT_SLASH
                break; // 
            case 31:
                //PT_CLOSEBL
                //PT_CLOSEBL
                break; // 
            case 32:
                //PT_NOTXOT
                //PT_NOTXOT
                break; // 
            case 33:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 70;
                else if (ch >= L'm' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 71;
                else if (ch == L't')
                    return 63;
                else if (ch == L'u')
                    return 72;
                else if (ch >= L'v' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 34:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 73;
                else if (ch >= L'p' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 74;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 35:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 75;
                else if (ch >= L'b' && ch <= L'g')
                    return 63;
                else if (ch == L'h')
                    return 76;
                else if (ch >= L'i' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 77;
                else if (ch >= L'm' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 78;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 36:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 79;
                else if (ch >= L'f' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 80;
                else if (ch >= L'p' && ch <= L'x')
                    return 63;
                else if (ch == L'y')
                    return 81;
                else if (ch == L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 37:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 82;
                else if (ch == L'm')
                    return 63;
                else if (ch == L'n')
                    return 83;
                else if (ch >= L'o' && ch <= L'w')
                    return 63;
                else if (ch == L'x')
                    return 84;
                else if (ch >= L'y' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 38:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 85;
                else if (ch >= L'b' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 86;
                else if (ch >= L'm' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 87;
                else if (ch >= L'p' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 88;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 39:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 89;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 40:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'e')
                    return 63;
                else if (ch == L'f')
                    return 90;
                else if (ch >= L'g' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 91;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 41:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 92;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 42:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L't')
                    return 63;
                else if (ch == L'u')
                    return 93;
                else if (ch >= L'v' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 43:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 94;
                else if (ch >= L'b' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 95;
                else if (ch >= L'f' && ch <= L't')
                    return 63;
                else if (ch == L'u')
                    return 96;
                else if (ch >= L'v' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 44:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'o')
                    return 63;
                else if (ch == L'p')
                    return 97;
                else if (ch >= L'q' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 45:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 98;
                else if (ch >= L's' && ch <= L't')
                    return 63;
                else if (ch == L'u')
                    return 99;
                else if (ch >= L'v' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 46:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 100;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 47:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'g')
                    return 63;
                else if (ch == L'h')
                    return 101;
                else if (ch == L'i')
                    return 102;
                else if (ch >= L'j' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 103;
                else if (ch >= L'u' && ch <= L'v')
                    return 63;
                else if (ch == L'w')
                    return 104;
                else if (ch >= L'x' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 48:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 105;
                else if (ch >= L'f' && ch <= L'g')
                    return 63;
                else if (ch == L'h')
                    return 106;
                else if (ch >= L'i' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 107;
                else if (ch >= L's' && ch <= L'x')
                    return 63;
                else if (ch == L'y')
                    return 108;
                else if (ch == L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 49:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 109;
                else if (ch >= L'o' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 110;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 50:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 111;
                else if (ch >= L'j' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 112;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 51:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 113;
                else if (ch >= L'd' && ch <= L'g')
                    return 63;
                else if (ch == L'h')
                    return 114;
                else if (ch >= L'i' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 52:
                //PT_OPENB
                //PT_OPENB
                break; // 
            case 53:
                //PT_OR
                //PT_OR
                break; // 
            case 54:
                if (ch == L'}')
                    return 115;
                //PT_CLOSEB
                //PT_CLOSEB
                break; // 
            case 55:
                //PT_NOTBIT
                //PT_NOTBIT
                break; // 
            case 56:
                return -1;
                break; // 
            case 57:
                if (ch == L' ')
                    return 57;
                //SPACES
                break; // 
            case 58:
                if (ch >= L' ' && ch <= L'!')
                    return 58;
                else if (ch == L'"')
                    return 59;
                else if (ch >= L'#' && ch <= L'[')
                    return 58;
                else if (ch == L'\\')
                    return 60;
                else if (ch >= L']' /*&& ch <= L'\u007f'*/)
                    return 58;
                break; // 
            case 59:
                //STRING
                break; // 
            case 60:
                return 58; //any
                break; // 
            case 61:
                if (ch == L' ')
                    return 61;
                else if (ch >= L'A' && ch <= L'Z')
                    return 62;
                else if (ch >= L'a' && ch <= L'z')
                    return 62;
                break; // 
            case 62:
                if (ch >= L'A' && ch <= L'Z')
                    return 62;
                else if (ch >= L'a' && ch <= L'z')
                    return 62;
                //DEFINE
                break; // 
            case 63:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 64:
                if (ch == L'$')
                    return 116;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 116;
                else if (ch == L'_')
                    return 116;
                else if (ch >= L'a' && ch <= L'z')
                    return 116;
                break; // 
            case 65:
                if (ch >= L' ' && ch <= L'&')
                    return 65;
                else if (ch == L'\'')
                    return 66;
                else if (ch >= L'(' && ch <= L'[')
                    return 65;
                else if (ch == L'\\')
                    return 67;
                else if (ch >= L']' /*&& ch <= L'\u007f'*/)
                    return 65;
                break; // 
            case 66:
                //CHAR
                break; // 
            case 67:
                return 65; //any
                break; // 
            case 68:
                if (ch == L'.')
                    return 69;
                else if (ch >= L'0' && ch <= L'9')
                    return 68;
                //NUMBER
                break; // 
            case 69:
                if (ch >= L'0' && ch <= L'9')
                    return 117;
                break; // 
            case 70:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 118;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 71:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'l')
                    return 63;
                else if (ch == L'm')
                    return 119;
                else if (ch >= L'n' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 72:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 120;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 73:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 121;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 74:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 122;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 75:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 123;
                else if (ch == L't')
                    return 124;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 76:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 125;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 77:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 126;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 78:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 127;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 79:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 128;
                else if (ch >= L'd' && ch <= L'e')
                    return 63;
                else if (ch == L'f')
                    return 129;
                else if (ch >= L'g' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 130;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 80:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L't')
                    return 63;
                else if (ch == L'u')
                    return 131;
                else if (ch >= L'v' && ch <= L'z')
                    return 63;
                //KY_DO
                //KY_DO
                break; // 
            case 81:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 132;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 82:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 133;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 83:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L't')
                    return 63;
                else if (ch == L'u')
                    return 134;
                else if (ch >= L'v' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 84:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'o')
                    return 63;
                else if (ch == L'p')
                    return 135;
                else if (ch >= L'q' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 136;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 85:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 137;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 86:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 138;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 87:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 139;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 88:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 140;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 89:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 141;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 90:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_IF
                //KY_IF
                break; // 
            case 91:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 142;
                else if (ch >= L'm' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 143;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 92:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 144;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 93:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 145;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 94:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'l')
                    return 63;
                else if (ch == L'm')
                    return 146;
                else if (ch >= L'n' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 95:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'v')
                    return 63;
                else if (ch == L'w')
                    return 147;
                else if (ch >= L'x' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 96:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 148;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 97:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 149;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 98:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 150;
                else if (ch >= L'j' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 151;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 99:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 63;
                else if (ch == L'b')
                    return 152;
                else if (ch >= L'c' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 100:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'f')
                    return 63;
                else if (ch == L'g')
                    return 153;
                else if (ch == L'h')
                    return 63;
                else if (ch == L'i')
                    return 154;
                else if (ch >= L'j' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 155;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 101:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 156;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 102:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'f')
                    return 63;
                else if (ch == L'g')
                    return 157;
                else if (ch >= L'h' && ch <= L'y')
                    return 63;
                else if (ch == L'z')
                    return 158;
                //SYMBOL
                break; // 
            case 103:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 159;
                else if (ch >= L'b' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 160;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 104:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 161;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 105:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'l')
                    return 63;
                else if (ch == L'm')
                    return 162;
                else if (ch >= L'n' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 106:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 163;
                else if (ch >= L'j' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 164;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 107:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L't')
                    return 63;
                else if (ch == L'u')
                    return 165;
                else if (ch >= L'v' && ch <= L'x')
                    return 63;
                else if (ch == L'y')
                    return 166;
                else if (ch == L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 108:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'o')
                    return 63;
                else if (ch == L'p')
                    return 167;
                else if (ch >= L'q' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 109:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 168;
                else if (ch >= L'j' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 169;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 110:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 170;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 111:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 171;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 112:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 172;
                else if (ch >= L'j' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 173;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 113:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'g')
                    return 63;
                else if (ch == L'h')
                    return 174;
                else if (ch >= L'i' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 114:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 175;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 115:
                if (ch == L'}')
                    return 176;
                break; // 
            case 116:
                if (ch == L'$')
                    return 116;
                else if (ch >= L'A' && ch <= L'Z')
                    return 116;
                else if (ch == L'_')
                    return 116;
                else if (ch >= L'a' && ch <= L'z')
                    return 116;
                //SYMBOL
                break; // 
            case 117:
                if (ch >= L'0' && ch <= L'9')
                    return 117;
                //NUMBER
                break; // 
            case 118:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'f')
                    return 63;
                else if (ch == L'g')
                    return 177;
                else if (ch >= L'h' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 119:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_ASM
                //KY_ASM
                break; // 
            case 120:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 178;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 121:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 179;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 122:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 180;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 123:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 181;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 124:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 182;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 125:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 183;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 126:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 184;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 127:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 185;
                else if (ch == L't')
                    return 186;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 128:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 187;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 129:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 188;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 130:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 189;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 131:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 63;
                else if (ch == L'b')
                    return 190;
                else if (ch >= L'c' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 132:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 191;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 133:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 192;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 134:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'l')
                    return 63;
                else if (ch == L'm')
                    return 193;
                else if (ch >= L'n' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 135:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 194;
                else if (ch >= L'm' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 195;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 136:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 196;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 137:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 197;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 138:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 198;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 139:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_FOR
                //KY_FOR
                break; // 
            case 140:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 199;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 141:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 200;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 142:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 201;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 143:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_INT
                //KY_INT
                break; // 
            case 144:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'f')
                    return 63;
                else if (ch == L'g')
                    return 202;
                else if (ch >= L'h' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 145:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 203;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 146:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 204;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 147:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_NEW
                //KY_NEW
                break; // 
            case 148:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 205;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 149:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 206;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 150:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'u')
                    return 63;
                else if (ch == L'v')
                    return 207;
                else if (ch >= L'w' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 151:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 208;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 152:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 209;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 153:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 210;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 154:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 211;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 155:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L't')
                    return 63;
                else if (ch == L'u')
                    return 212;
                else if (ch >= L'v' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 156:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 213;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 157:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 214;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 158:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 215;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 159:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 216;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 160:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L't')
                    return 63;
                else if (ch == L'u')
                    return 217;
                else if (ch >= L'v' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 161:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 218;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 162:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'o')
                    return 63;
                else if (ch == L'p')
                    return 219;
                else if (ch >= L'q' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 163:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 220;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 164:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 221;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 165:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 222;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 166:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_TRY
                //KY_TRY
                break; // 
            case 167:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 223;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 168:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 224;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 169:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 225;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 170:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 226;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 171:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 227;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 172:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'c')
                    return 63;
                else if (ch == L'd')
                    return 228;
                else if (ch >= L'e' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 173:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 229;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 174:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 230;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 175:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 231;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 176:
                //NOWIKI_BLOCK_CLOSE
                break; // 
            case 177:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 232;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 178:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_AUTO
                //KY_AUTO
                break; // 
            case 179:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_BOOL
                //KY_BOOL
                break; // 
            case 180:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'j')
                    return 63;
                else if (ch == L'k')
                    return 233;
                else if (ch >= L'l' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 181:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_CASE
                //KY_CASE
                break; // 
            case 182:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'g')
                    return 63;
                else if (ch == L'h')
                    return 234;
                else if (ch >= L'i' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 183:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_CHAR
                //KY_CHAR
                break; // 
            case 184:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 235;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 185:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 236;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 186:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 237;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 187:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 238;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 188:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L't')
                    return 63;
                else if (ch == L'u')
                    return 239;
                else if (ch >= L'v' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 189:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 240;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 190:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 241;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 191:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'l')
                    return 63;
                else if (ch == L'm')
                    return 242;
                else if (ch >= L'n' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 192:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_ELSE
                //KY_ELSE
                break; // 
            case 193:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_ENUM
                //KY_ENUM
                break; // 
            case 194:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 243;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 195:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 244;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 196:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 245;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 197:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 246;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 198:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 247;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 199:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 248;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 200:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_GOTO
                //KY_GOTO
                break; // 
            case 201:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 249;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 202:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_LONG
                //KY_LONG
                break; // 
            case 203:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 63;
                else if (ch == L'b')
                    return 250;
                else if (ch >= L'c' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 204:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 251;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 205:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'o')
                    return 63;
                else if (ch == L'p')
                    return 252;
                else if (ch >= L'q' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 206:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 253;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 207:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 254;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 208:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 255;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 209:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 256;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 210:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 257;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 211:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 258;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 212:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 259;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 213:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 260;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 214:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 261;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 215:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 262;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 216:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 263;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 217:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 264;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 218:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 265;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 219:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 266;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 220:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_THIS
                //KY_THIS
                break; // 
            case 221:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'v')
                    return 63;
                else if (ch == L'w')
                    return 267;
                else if (ch >= L'x' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 222:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_TRUE
                //KY_TRUE
                break; // 
            case 223:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'c')
                    return 63;
                else if (ch == L'd')
                    return 268;
                else if (ch >= L'e' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 269;
                else if (ch >= L'j' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 270;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 224:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 271;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 225:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'f')
                    return 63;
                else if (ch == L'g')
                    return 272;
                else if (ch >= L'h' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 226:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'f')
                    return 63;
                else if (ch == L'g')
                    return 273;
                else if (ch >= L'h' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 227:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L't')
                    return 63;
                else if (ch == L'u')
                    return 274;
                else if (ch >= L'v' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 228:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_VOID
                //KY_VOID
                break; // 
            case 229:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 275;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 230:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 276;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 231:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 277;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 232:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 278;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 233:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_BREAK
                //KY_BREAK
                break; // 
            case 234:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_CATCH
                //KY_CATCH
                break; // 
            case 235:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_CLASS
                //KY_CLASS
                break; // 
            case 236:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 279;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 280;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //KY_CONST
                //KY_CONST
                break; // 
            case 237:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 281;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 238:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'x')
                    return 63;
                else if (ch == L'y')
                    return 282;
                else if (ch == L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 239:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 283;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 240:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 284;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 241:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 285;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 242:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 286;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 243:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 287;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 244:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 288;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 245:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 289;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 246:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_FALSE
                //KY_FALSE
                break; // 
            case 247:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_FLOAT
                //KY_FLOAT
                break; // 
            case 248:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'c')
                    return 63;
                else if (ch == L'd')
                    return 290;
                else if (ch >= L'e' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 249:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 291;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 250:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 292;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 251:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'o')
                    return 63;
                else if (ch == L'p')
                    return 293;
                else if (ch >= L'q' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 252:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 294;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 253:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 295;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 254:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 296;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 255:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 297;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 256:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 298;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 257:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 299;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 258:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 300;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 259:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 301;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 260:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_SHORT
                //KY_SHORT
                break; // 
            case 261:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'c')
                    return 63;
                else if (ch == L'd')
                    return 302;
                else if (ch >= L'e' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 262:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'e')
                    return 63;
                else if (ch == L'f')
                    return 303;
                else if (ch >= L'g' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 263:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 304;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 264:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 305;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 265:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'g')
                    return 63;
                else if (ch == L'h')
                    return 306;
                else if (ch >= L'i' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 266:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 307;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 267:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_THROW
                //KY_THROW
                break; // 
            case 268:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 308;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 269:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'c')
                    return 63;
                else if (ch == L'd')
                    return 309;
                else if (ch >= L'e' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 270:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 310;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 271:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_UNION
                //KY_UNION
                break; // 
            case 272:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'm')
                    return 63;
                else if (ch == L'n')
                    return 311;
                else if (ch >= L'o' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 273:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_USING
                //KY_USING
                break; // 
            case 274:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 312;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 275:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 313;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 276:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 314;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 277:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_WHILE
                //KY_WHILE
                break; // 
            case 278:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'e')
                    return 63;
                else if (ch == L'f')
                    return 315;
                else if (ch >= L'g' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 279:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 316;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 280:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'w')
                    return 63;
                else if (ch == L'x')
                    return 317;
                else if (ch >= L'y' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 281:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L't')
                    return 63;
                else if (ch == L'u')
                    return 318;
                else if (ch >= L'v' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 282:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'o')
                    return 63;
                else if (ch == L'p')
                    return 319;
                else if (ch >= L'q' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 283:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 320;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 284:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_DELETE
                //KY_DELETE
                break; // 
            case 285:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_DOUBLE
                //KY_DOUBLE
                break; // 
            case 286:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 321;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 287:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'h')
                    return 63;
                else if (ch == L'i')
                    return 322;
                else if (ch >= L'j' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 288:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_EXPORT
                //KY_EXPORT
                break; // 
            case 289:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_EXTERN
                //KY_EXTERN
                break; // 
            case 290:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_FRIEND
                //KY_FRIEND
                break; // 
            case 291:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_INLINE
                //KY_INLINE
                break; // 
            case 292:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 323;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 293:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 324;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 294:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 325;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 295:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'n')
                    return 63;
                else if (ch == L'o')
                    return 326;
                else if (ch >= L'p' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 296:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 327;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 297:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 328;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 298:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_PUBLIC
                //KY_PUBLIC
                break; // 
            case 299:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 329;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 300:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 330;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 301:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_RETURN
                //KY_RETURN
                break; // 
            case 302:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_SIGNED
                //KY_SIGNED
                break; // 
            case 303:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_SIZEOF
                //KY_SIZEOF
                break; // 
            case 304:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 331;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_STATIC
                //KY_STATIC
                break; // 
            case 305:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_STRUCT
                //KY_STRUCT
                break; // 
            case 306:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_SWITCH
                //KY_SWITCH
                break; // 
            case 307:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 332;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 308:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'e')
                    return 63;
                else if (ch == L'f')
                    return 333;
                else if (ch >= L'g' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 309:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_TYPEID
                //KY_TYPEID
                break; // 
            case 310:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'l')
                    return 63;
                else if (ch == L'm')
                    return 334;
                else if (ch >= L'n' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 311:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 335;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 312:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 336;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 313:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'k')
                    return 63;
                else if (ch == L'l')
                    return 337;
                else if (ch >= L'm' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 314:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 338;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 315:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_ALIGNOF
                //KY_ALIGNOF
                break; // 
            case 316:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 339;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 317:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'o')
                    return 63;
                else if (ch == L'p')
                    return 340;
                else if (ch >= L'q' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 318:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 341;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 319:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 342;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 320:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_DEFAULT
                //KY_DEFAULT
                break; // 
            case 321:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 343;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 322:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 344;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 323:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_MUTABLE
                //KY_MUTABLE
                break; // 
            case 324:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 345;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 325:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_NULL
                //KY_NULL
                break; // 
            case 326:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 346;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 327:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_PRIVATE
                //KY_PRIVATE
                break; // 
            case 328:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 347;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 329:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 348;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 330:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'o')
                    return 63;
                else if (ch == L'p')
                    return 349;
                else if (ch >= L'q' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 331:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 350;
                else if (ch == L'b')
                    return 63;
                else if (ch == L'c')
                    return 351;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 332:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 352;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 333:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_TYPEDEF
                //KY_TYPEDEF
                break; // 
            case 334:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 353;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 335:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'c')
                    return 63;
                else if (ch == L'd')
                    return 354;
                else if (ch >= L'e' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 336:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_VIRTUAL
                //KY_VIRTUAL
                break; // 
            case 337:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 355;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 338:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_WCHAR_T
                //KY_WCHAR_T
                break; // 
            case 339:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 356;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 340:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 357;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 341:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_CONTINUE
                //KY_CONTINUE
                break; // 
            case 342:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_DECLTYPE
                //KY_DECLTYPE
                break; // 
            case 343:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 358;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 344:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_EXPLICIT
                //KY_EXPLICIT
                break; // 
            case 345:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 359;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 346:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_OPERATOR
                //KY_OPERATOR
                break; // 
            case 347:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'c')
                    return 63;
                else if (ch == L'd')
                    return 360;
                else if (ch >= L'e' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 348:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_REGISTER
                //KY_REGISTER
                break; // 
            case 349:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 361;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 350:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 362;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 351:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 363;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 352:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_TEMPLATE
                //KY_TEMPLATE
                break; // 
            case 353:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_TYPENAME
                //KY_TYPENAME
                break; // 
            case 354:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_UNSIGNED
                //KY_UNSIGNED
                break; // 
            case 355:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_VOLATILE
                //KY_VOLATILE
                break; // 
            case 356:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 364;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 357:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_CONSTEXPR
                //KY_CONSTEXPR
                break; // 
            case 358:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 365;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 359:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_NAMESPACE
                //KY_NAMESPACE
                break; // 
            case 360:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_PROTECTED
                //KY_PROTECTED
                break; // 
            case 361:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 366;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 362:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 367;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 363:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 368;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 364:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_CONST_CAST
                //KY_CONST_CAST
                break; // 
            case 365:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 369;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 366:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 370;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 367:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'd')
                    return 63;
                else if (ch == L'e')
                    return 371;
                else if (ch >= L'f' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 368:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 372;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 369:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 373;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 370:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 374;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 371:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'q')
                    return 63;
                else if (ch == L'r')
                    return 375;
                else if (ch >= L's' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 372:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_STATIC_CAST
                //KY_STATIC_CAST
                break; // 
            case 373:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_DYNAMIC_CAST
                //KY_DYNAMIC_CAST
                break; // 
            case 374:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'b')
                    return 63;
                else if (ch == L'c')
                    return 376;
                else if (ch >= L'd' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 375:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 377;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 376:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch == L'a')
                    return 378;
                else if (ch >= L'b' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 377:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_STATIC_ASSERT
                //KY_STATIC_ASSERT
                break; // 
            case 378:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'r')
                    return 63;
                else if (ch == L's')
                    return 379;
                else if (ch >= L't' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 379:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L's')
                    return 63;
                else if (ch == L't')
                    return 380;
                else if (ch >= L'u' && ch <= L'z')
                    return 63;
                //SYMBOL
                break; // 
            case 380:
                if (ch == L'$')
                    return 63;
                else if (ch >= L'0' && ch <= L'9')
                    return 64;
                else if (ch >= L'A' && ch <= L'Z')
                    return 63;
                else if (ch == L'_')
                    return 63;
                else if (ch >= L'a' && ch <= L'z')
                    return 63;
                //KY_REINTERPRET_CAST
                //KY_REINTERPRET_CAST
                break; // 
            } //switch
            return -1;
        }

        static bool IsInterleave(Tokens tk)
        {
            return false; //todo add you blanks token here
        }

        static bool GetTokenFromState(int state, Tokens& tk)
        {
            switch (state)
            {
            case 1: tk = T_EOF; break;
            case 2: tk = TAB; break;
            case 3: tk = NEWLINE; break;
            case 5: tk = SPACES; break;
            case 6: tk = PT_NOT; break;
            case 7: tk = PT_DOUBLEQ; break;
            case 8: tk = PT_NUMBER; break;
            case 9: tk = SYMBOL; break;
            case 10: tk = PT_PERCENT; break;
            case 11: tk = PT_AND; break;
            case 12: tk = PT_SINGLEQ; break;
            case 13: tk = PT_OPENP; break;
            case 14: tk = PT_CLOSEP; break;
            case 15: tk = PT_MULTI; break;
            case 16: tk = PT_PLUS; break;
            case 17: tk = PT_COMA; break;
            case 18: tk = PT_MINUS; break;
            case 19: tk = PT_DOT; break;
            case 20: tk = PT_INVSLASH; break;
            case 21: tk = NUMBER; break;
            case 22: tk = PT_DOTDOT; break;
            case 23: tk = PT_DOTCOMA; break;
            case 24: tk = PT_LESSTHAN; break;
            case 25: tk = PT_EQUAL; break;
            case 26: tk = PT_MORETHAN; break;
            case 27: tk = PT_QUESTION; break;
            case 28: tk = INSIGNIFICANT_CHAR; break;
            case 29: tk = PT_OPENBL; break;
            case 30: tk = PT_SLASH; break;
            case 31: tk = PT_CLOSEBL; break;
            case 32: tk = PT_NOTXOT; break;
            case 33: tk = SYMBOL; break;
            case 34: tk = SYMBOL; break;
            case 35: tk = SYMBOL; break;
            case 36: tk = SYMBOL; break;
            case 37: tk = SYMBOL; break;
            case 38: tk = SYMBOL; break;
            case 39: tk = SYMBOL; break;
            case 40: tk = SYMBOL; break;
            case 41: tk = SYMBOL; break;
            case 42: tk = SYMBOL; break;
            case 43: tk = SYMBOL; break;
            case 44: tk = SYMBOL; break;
            case 45: tk = SYMBOL; break;
            case 46: tk = SYMBOL; break;
            case 47: tk = SYMBOL; break;
            case 48: tk = SYMBOL; break;
            case 49: tk = SYMBOL; break;
            case 50: tk = SYMBOL; break;
            case 51: tk = SYMBOL; break;
            case 52: tk = PT_OPENB; break;
            case 53: tk = PT_OR; break;
            case 54: tk = PT_CLOSEB; break;
            case 55: tk = PT_NOTBIT; break;
            case 57: tk = SPACES; break;
            case 59: tk = STRING; break;
            case 62: tk = DEFINE; break;
            case 63: tk = SYMBOL; break;
            case 66: tk = CHAR; break;
            case 68: tk = NUMBER; break;
            case 70: tk = SYMBOL; break;
            case 71: tk = SYMBOL; break;
            case 72: tk = SYMBOL; break;
            case 73: tk = SYMBOL; break;
            case 74: tk = SYMBOL; break;
            case 75: tk = SYMBOL; break;
            case 76: tk = SYMBOL; break;
            case 77: tk = SYMBOL; break;
            case 78: tk = SYMBOL; break;
            case 79: tk = SYMBOL; break;
            case 80: tk = KY_DO; break;
            case 81: tk = SYMBOL; break;
            case 82: tk = SYMBOL; break;
            case 83: tk = SYMBOL; break;
            case 84: tk = SYMBOL; break;
            case 85: tk = SYMBOL; break;
            case 86: tk = SYMBOL; break;
            case 87: tk = SYMBOL; break;
            case 88: tk = SYMBOL; break;
            case 89: tk = SYMBOL; break;
            case 90: tk = KY_IF; break;
            case 91: tk = SYMBOL; break;
            case 92: tk = SYMBOL; break;
            case 93: tk = SYMBOL; break;
            case 94: tk = SYMBOL; break;
            case 95: tk = SYMBOL; break;
            case 96: tk = SYMBOL; break;
            case 97: tk = SYMBOL; break;
            case 98: tk = SYMBOL; break;
            case 99: tk = SYMBOL; break;
            case 100: tk = SYMBOL; break;
            case 101: tk = SYMBOL; break;
            case 102: tk = SYMBOL; break;
            case 103: tk = SYMBOL; break;
            case 104: tk = SYMBOL; break;
            case 105: tk = SYMBOL; break;
            case 106: tk = SYMBOL; break;
            case 107: tk = SYMBOL; break;
            case 108: tk = SYMBOL; break;
            case 109: tk = SYMBOL; break;
            case 110: tk = SYMBOL; break;
            case 111: tk = SYMBOL; break;
            case 112: tk = SYMBOL; break;
            case 113: tk = SYMBOL; break;
            case 114: tk = SYMBOL; break;
            case 116: tk = SYMBOL; break;
            case 117: tk = NUMBER; break;
            case 118: tk = SYMBOL; break;
            case 119: tk = KY_ASM; break;
            case 120: tk = SYMBOL; break;
            case 121: tk = SYMBOL; break;
            case 122: tk = SYMBOL; break;
            case 123: tk = SYMBOL; break;
            case 124: tk = SYMBOL; break;
            case 125: tk = SYMBOL; break;
            case 126: tk = SYMBOL; break;
            case 127: tk = SYMBOL; break;
            case 128: tk = SYMBOL; break;
            case 129: tk = SYMBOL; break;
            case 130: tk = SYMBOL; break;
            case 131: tk = SYMBOL; break;
            case 132: tk = SYMBOL; break;
            case 133: tk = SYMBOL; break;
            case 134: tk = SYMBOL; break;
            case 135: tk = SYMBOL; break;
            case 136: tk = SYMBOL; break;
            case 137: tk = SYMBOL; break;
            case 138: tk = SYMBOL; break;
            case 139: tk = KY_FOR; break;
            case 140: tk = SYMBOL; break;
            case 141: tk = SYMBOL; break;
            case 142: tk = SYMBOL; break;
            case 143: tk = KY_INT; break;
            case 144: tk = SYMBOL; break;
            case 145: tk = SYMBOL; break;
            case 146: tk = SYMBOL; break;
            case 147: tk = KY_NEW; break;
            case 148: tk = SYMBOL; break;
            case 149: tk = SYMBOL; break;
            case 150: tk = SYMBOL; break;
            case 151: tk = SYMBOL; break;
            case 152: tk = SYMBOL; break;
            case 153: tk = SYMBOL; break;
            case 154: tk = SYMBOL; break;
            case 155: tk = SYMBOL; break;
            case 156: tk = SYMBOL; break;
            case 157: tk = SYMBOL; break;
            case 158: tk = SYMBOL; break;
            case 159: tk = SYMBOL; break;
            case 160: tk = SYMBOL; break;
            case 161: tk = SYMBOL; break;
            case 162: tk = SYMBOL; break;
            case 163: tk = SYMBOL; break;
            case 164: tk = SYMBOL; break;
            case 165: tk = SYMBOL; break;
            case 166: tk = KY_TRY; break;
            case 167: tk = SYMBOL; break;
            case 168: tk = SYMBOL; break;
            case 169: tk = SYMBOL; break;
            case 170: tk = SYMBOL; break;
            case 171: tk = SYMBOL; break;
            case 172: tk = SYMBOL; break;
            case 173: tk = SYMBOL; break;
            case 174: tk = SYMBOL; break;
            case 175: tk = SYMBOL; break;
            case 176: tk = NOWIKI_BLOCK_CLOSE; break;
            case 177: tk = SYMBOL; break;
            case 178: tk = KY_AUTO; break;
            case 179: tk = KY_BOOL; break;
            case 180: tk = SYMBOL; break;
            case 181: tk = KY_CASE; break;
            case 182: tk = SYMBOL; break;
            case 183: tk = KY_CHAR; break;
            case 184: tk = SYMBOL; break;
            case 185: tk = SYMBOL; break;
            case 186: tk = SYMBOL; break;
            case 187: tk = SYMBOL; break;
            case 188: tk = SYMBOL; break;
            case 189: tk = SYMBOL; break;
            case 190: tk = SYMBOL; break;
            case 191: tk = SYMBOL; break;
            case 192: tk = KY_ELSE; break;
            case 193: tk = KY_ENUM; break;
            case 194: tk = SYMBOL; break;
            case 195: tk = SYMBOL; break;
            case 196: tk = SYMBOL; break;
            case 197: tk = SYMBOL; break;
            case 198: tk = SYMBOL; break;
            case 199: tk = SYMBOL; break;
            case 200: tk = KY_GOTO; break;
            case 201: tk = SYMBOL; break;
            case 202: tk = KY_LONG; break;
            case 203: tk = SYMBOL; break;
            case 204: tk = SYMBOL; break;
            case 205: tk = SYMBOL; break;
            case 206: tk = SYMBOL; break;
            case 207: tk = SYMBOL; break;
            case 208: tk = SYMBOL; break;
            case 209: tk = SYMBOL; break;
            case 210: tk = SYMBOL; break;
            case 211: tk = SYMBOL; break;
            case 212: tk = SYMBOL; break;
            case 213: tk = SYMBOL; break;
            case 214: tk = SYMBOL; break;
            case 215: tk = SYMBOL; break;
            case 216: tk = SYMBOL; break;
            case 217: tk = SYMBOL; break;
            case 218: tk = SYMBOL; break;
            case 219: tk = SYMBOL; break;
            case 220: tk = KY_THIS; break;
            case 221: tk = SYMBOL; break;
            case 222: tk = KY_TRUE; break;
            case 223: tk = SYMBOL; break;
            case 224: tk = SYMBOL; break;
            case 225: tk = SYMBOL; break;
            case 226: tk = SYMBOL; break;
            case 227: tk = SYMBOL; break;
            case 228: tk = KY_VOID; break;
            case 229: tk = SYMBOL; break;
            case 230: tk = SYMBOL; break;
            case 231: tk = SYMBOL; break;
            case 232: tk = SYMBOL; break;
            case 233: tk = KY_BREAK; break;
            case 234: tk = KY_CATCH; break;
            case 235: tk = KY_CLASS; break;
            case 236: tk = KY_CONST; break;
            case 237: tk = SYMBOL; break;
            case 238: tk = SYMBOL; break;
            case 239: tk = SYMBOL; break;
            case 240: tk = SYMBOL; break;
            case 241: tk = SYMBOL; break;
            case 242: tk = SYMBOL; break;
            case 243: tk = SYMBOL; break;
            case 244: tk = SYMBOL; break;
            case 245: tk = SYMBOL; break;
            case 246: tk = KY_FALSE; break;
            case 247: tk = KY_FLOAT; break;
            case 248: tk = SYMBOL; break;
            case 249: tk = SYMBOL; break;
            case 250: tk = SYMBOL; break;
            case 251: tk = SYMBOL; break;
            case 252: tk = SYMBOL; break;
            case 253: tk = SYMBOL; break;
            case 254: tk = SYMBOL; break;
            case 255: tk = SYMBOL; break;
            case 256: tk = SYMBOL; break;
            case 257: tk = SYMBOL; break;
            case 258: tk = SYMBOL; break;
            case 259: tk = SYMBOL; break;
            case 260: tk = KY_SHORT; break;
            case 261: tk = SYMBOL; break;
            case 262: tk = SYMBOL; break;
            case 263: tk = SYMBOL; break;
            case 264: tk = SYMBOL; break;
            case 265: tk = SYMBOL; break;
            case 266: tk = SYMBOL; break;
            case 267: tk = KY_THROW; break;
            case 268: tk = SYMBOL; break;
            case 269: tk = SYMBOL; break;
            case 270: tk = SYMBOL; break;
            case 271: tk = KY_UNION; break;
            case 272: tk = SYMBOL; break;
            case 273: tk = KY_USING; break;
            case 274: tk = SYMBOL; break;
            case 275: tk = SYMBOL; break;
            case 276: tk = SYMBOL; break;
            case 277: tk = KY_WHILE; break;
            case 278: tk = SYMBOL; break;
            case 279: tk = SYMBOL; break;
            case 280: tk = SYMBOL; break;
            case 281: tk = SYMBOL; break;
            case 282: tk = SYMBOL; break;
            case 283: tk = SYMBOL; break;
            case 284: tk = KY_DELETE; break;
            case 285: tk = KY_DOUBLE; break;
            case 286: tk = SYMBOL; break;
            case 287: tk = SYMBOL; break;
            case 288: tk = KY_EXPORT; break;
            case 289: tk = KY_EXTERN; break;
            case 290: tk = KY_FRIEND; break;
            case 291: tk = KY_INLINE; break;
            case 292: tk = SYMBOL; break;
            case 293: tk = SYMBOL; break;
            case 294: tk = SYMBOL; break;
            case 295: tk = SYMBOL; break;
            case 296: tk = SYMBOL; break;
            case 297: tk = SYMBOL; break;
            case 298: tk = KY_PUBLIC; break;
            case 299: tk = SYMBOL; break;
            case 300: tk = SYMBOL; break;
            case 301: tk = KY_RETURN; break;
            case 302: tk = KY_SIGNED; break;
            case 303: tk = KY_SIZEOF; break;
            case 304: tk = KY_STATIC; break;
            case 305: tk = KY_STRUCT; break;
            case 306: tk = KY_SWITCH; break;
            case 307: tk = SYMBOL; break;
            case 308: tk = SYMBOL; break;
            case 309: tk = KY_TYPEID; break;
            case 310: tk = SYMBOL; break;
            case 311: tk = SYMBOL; break;
            case 312: tk = SYMBOL; break;
            case 313: tk = SYMBOL; break;
            case 314: tk = SYMBOL; break;
            case 315: tk = KY_ALIGNOF; break;
            case 316: tk = SYMBOL; break;
            case 317: tk = SYMBOL; break;
            case 318: tk = SYMBOL; break;
            case 319: tk = SYMBOL; break;
            case 320: tk = KY_DEFAULT; break;
            case 321: tk = SYMBOL; break;
            case 322: tk = SYMBOL; break;
            case 323: tk = KY_MUTABLE; break;
            case 324: tk = SYMBOL; break;
            case 325: tk = KY_NULL; break;
            case 326: tk = SYMBOL; break;
            case 327: tk = KY_PRIVATE; break;
            case 328: tk = SYMBOL; break;
            case 329: tk = SYMBOL; break;
            case 330: tk = SYMBOL; break;
            case 331: tk = SYMBOL; break;
            case 332: tk = SYMBOL; break;
            case 333: tk = KY_TYPEDEF; break;
            case 334: tk = SYMBOL; break;
            case 335: tk = SYMBOL; break;
            case 336: tk = KY_VIRTUAL; break;
            case 337: tk = SYMBOL; break;
            case 338: tk = KY_WCHAR_T; break;
            case 339: tk = SYMBOL; break;
            case 340: tk = SYMBOL; break;
            case 341: tk = KY_CONTINUE; break;
            case 342: tk = KY_DECLTYPE; break;
            case 343: tk = SYMBOL; break;
            case 344: tk = KY_EXPLICIT; break;
            case 345: tk = SYMBOL; break;
            case 346: tk = KY_OPERATOR; break;
            case 347: tk = SYMBOL; break;
            case 348: tk = KY_REGISTER; break;
            case 349: tk = SYMBOL; break;
            case 350: tk = SYMBOL; break;
            case 351: tk = SYMBOL; break;
            case 352: tk = KY_TEMPLATE; break;
            case 353: tk = KY_TYPENAME; break;
            case 354: tk = KY_UNSIGNED; break;
            case 355: tk = KY_VOLATILE; break;
            case 356: tk = SYMBOL; break;
            case 357: tk = KY_CONSTEXPR; break;
            case 358: tk = SYMBOL; break;
            case 359: tk = KY_NAMESPACE; break;
            case 360: tk = KY_PROTECTED; break;
            case 361: tk = SYMBOL; break;
            case 362: tk = SYMBOL; break;
            case 363: tk = SYMBOL; break;
            case 364: tk = KY_CONST_CAST; break;
            case 365: tk = SYMBOL; break;
            case 366: tk = SYMBOL; break;
            case 367: tk = SYMBOL; break;
            case 368: tk = SYMBOL; break;
            case 369: tk = SYMBOL; break;
            case 370: tk = SYMBOL; break;
            case 371: tk = SYMBOL; break;
            case 372: tk = KY_STATIC_CAST; break;
            case 373: tk = KY_DYNAMIC_CAST; break;
            case 374: tk = SYMBOL; break;
            case 375: tk = SYMBOL; break;
            case 376: tk = SYMBOL; break;
            case 377: tk = KY_STATIC_ASSERT; break;
            case 378: tk = SYMBOL; break;
            case 379: tk = SYMBOL; break;
            case 380: tk = KY_REINTERPRET_CAST; break;
            default:
                return false;
            }
            return true;
        }
    };
} //namespace
#endif //header
